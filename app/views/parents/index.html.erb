<%= link_to "Back to Options", current_user %>

<h4>Here are all the parents:</h4>

<table class="table">
  <thead>
    <tr>
      <th>
        Name
      </th>
      <th>
        Student
      </th>
    </tr>
  </thead>
  <tbody>
    <% @users.where(type: "Parent").each do |user| %>
    <tr>
      <td>
        <%= user.name %>
      </td>
      <td>
        <%= Student.find_by(id: user.student_id).name %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
