<h4>Enter the info for your new comment!</h4>

<%= form_for @comment do |f| %>
<%= label_tag :body %>
  <%= f.text_field :body, class: 'form-control' %>
  <hr>
  <%= f.submit class: 'btn btn-default btn-success' %>
<% end %>
